name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install devDependencies (eslint/prettier)
        run: npm ci
      - name: ESLint (only hard errors block; warnings are tracked debt)
        run: npm run lint

  pv-regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Playwright
        run: |
          pip install playwright
          python -m playwright install chromium --with-deps

      - name: Start local server (python server.py :8001)
        run: |
          python server.py > server.log 2>&1 &
          for i in $(seq 1 40); do
            if curl -sf http://localhost:8001/index.html > /dev/null 2>&1; then
              echo "server up on :8001"; exit 0
            fi
            sleep 1
          done
          echo "server failed to start, log:"; tail -50 server.log; exit 1

      - name: Run JS unit tests (parsers / particles / music fallback / nowplaying / escape-gate rule / stall detector)
        run: |
          node --test tests/js/test_parsers.js
          node --test tests/js/test_particles.js
          node --test tests/js/test_music_api_fallback.js
          node --test tests/js/test_nowplaying.js
          node --test tests/js/test_html_escape_rule.js
          node --test tests/js/test_stall_detector.js
          node --test tests/js/test_fade_controller.js
          node --test tests/js/test_shortcut_manager.js
          node --test tests/js/test_equalizer.js
          node --test tests/js/test_play_source.js
          node --test tests/js/test_shape_field.js
          node --test tests/js/test_pv_arc_field.js
          node --test tests/js/test_motion.js
          node --test tests/js/test_ai_lyrics_word_fallback.js
          node --test tests/js/test_i18n_coverage.js

      - name: Motion discipline ratchet (easing curves / bounce / transition:all / will-change)
        run: node scripts/audits/motion-audit.mjs --check

      - name: i18n phrase-registry ratchet (中文 UI 文本必须登记进 STATIC_PHRASE_MAP)
        run: node scripts/audits/i18n-coverage.mjs --check

      - name: Run Python unit tests (audio stream proxy / MIME)
        run: |
          python -m unittest tests.python.test_audio_stream

      - name: Run PV regression tests
        run: |
          python tests/test_pv_handoff.py
          python tests/test_pv_scene_group.py
          python tests/test_pv_geo_mosaic.py

      - name: Settings panel regression smoke
        run: |
          python tests/test_settings_smoke.py
          python tests/test_word_fallback.py